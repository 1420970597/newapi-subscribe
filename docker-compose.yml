services:
  subscribe:
    build: .
    container_name: newapi-subscribe
    ports:
      - "8090:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
